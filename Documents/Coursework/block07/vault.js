const name = "User";
let str1 = "Hello " + name + ". You have received this message because you have been chosen to open an important vault. Here is the secret combination:";
// Assigned gretting for user on page
let numLock1 = 5 * 2;
let numLock2 = 10 * 4;
let numLock3 = 35 + 4;
// Assigned combination, used arithmetic operators to calculate combinations for User
alert(str1 + ' ' + numLock1 + ' ' + numLock2 + ' ' + numLock3);
// Alerted User with greeting and combination